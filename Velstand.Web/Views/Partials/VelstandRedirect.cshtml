@inherits Umbraco.Web.Mvc.UmbracoTemplatePage
@{
    try
    {
        IPublishedContent current = CurrentPage;

        var content = current.FirstChild();
        Response.Redirect(content.Url, true);
    }
    catch
    {
        Response.Redirect("/", true);
    }
}